{
  "ignorePatterns": [
    { "pattern": "^mailto:" },
    { "pattern": "^https://github.com/.*/pull/" },
    { "pattern": "^http://localhost" },
    { "pattern": "^http://127\\.0\\.0\\.1" }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 2
}
